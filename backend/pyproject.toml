[project]
name = "discord-backend"
version = "0.1.0"
description = "Discord-like chat, voice and screensharing backend"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1",
    "channels>=4.1",
    "daphne>=4.1",
    "djangorestframework>=3.15",
    "django-cors-headers>=4.4",
    "google-genai>=2.25.0",
]
